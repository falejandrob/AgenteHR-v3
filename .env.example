# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=your-deployment-name

# Azure AI Search Configuration
AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
AZURE_SEARCH_KEY=your-search-api-key
AZURE_SEARCH_INDEX=your-index-name

# Azure AI Search Optional Configuration
# Solo agregar si tienes configuración semántica habilitada
# AZURE_SEARCH_SEMANTIC_CONFIG=default
# Forzar uso exclusivo de Azure Search (sin fallback a vectorstore local)
# AZURE_SEARCH_ONLY=true
# Activar vector search en Azure (usa campo content_embedding por defecto)
# AZURE_SEARCH_VECTOR=true
# Nombre del campo vectorial (si difiere)
# AZURE_SEARCH_VECTOR_FIELD=content_embedding
# Desactivar traducción (se desactiva automáticamente si vector search activo)
# DISABLE_TRANSLATION=true

# Application Configuration
PORT=3000
NODE_ENV=production

# Debug Mode (opcional)
# Establecer en 'development' para ver más detalles en los logs
# NODE_ENV=development